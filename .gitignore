# dependencies
node_modules/

# job storage + generated results (contain scraped personal data — never commit)
jobs/
.testdata/
*.engine.csv
cc-results.csv
cc-results-*.csv
tmp-results.csv
contacts.json
contacts.json.tmp
contacts.db
contacts.db-wal
contacts.db-shm
users.db
users.db-wal
users.db-shm

# spreadsheets contain some personal contacts/names — keep out of the repo
*.xlsx

# local run logs + scratch
*-run-output.txt
pipeline_run_output.txt
tmp-*.js

# secrets / local env
.env
.env.local

# os junk
.DS_Store
Thumbs.db

# local editor/agent settings
.claude/

# original logo drop (the served copy lives at ui/logo.png)
/Common Crawler.png
