# dependencies
node_modules/

# job storage + generated results (contain scraped personal data — never commit)
jobs/
.testdata/
*.engine.csv
cc-results.csv
cc-results-*.csv
contacts-database*.csv
cc-domains*.csv
tmp-results.csv
contacts.json
contacts.json.tmp
contacts.db
contacts.db-wal
contacts.db-shm
users.db
users.db-wal
users.db-shm

# spreadsheets contain some personal contacts/names — keep out of the repo
*.xlsx

# local run logs + scratch
*-run-output.txt
pipeline_run_output.txt
tmp-*.js

# secrets / local env
.env
.env.local

# os junk
.DS_Store
Thumbs.db

# local editor/agent settings
.claude/

# original logo drop (the served copy lives at ui/logo.png)
/Common Crawler.png

cc-bio-urls*.txt
cc-warc*.jsonl
bio-url-splits/
BIO URL Repository.txt

# Lambda build + harvest scratch
lambda-build/
test-ptrs.jsonl
ptrs-*-test.jsonl
names-genders.csv.bak

# Regenerable pipeline artifacts — staged URL lists, shard/chunk splits and harvest dumps.
# These were swept into 79e6c57 by a broad `git add` and made the pending push a 1.06GB pack that
# GitHub rejected with HTTP 408. They are all reproducible output, never inputs.
monitor-bio-urls.txt
monitor-misses.txt
live-candidates-full.txt
mbu-chunk-*
mmiss-chunk-*
mbu-sample.txt
mbu-sample-ptr.jsonl
live-shard-*
live-rr-*.txt
batch-1m.txt
bio-repo-misses.txt
wdc-emails-*.txt
_universe/u-targets.ndjson
_universe/u-urls.txt
logs/*.log

# Generated export (64MB) — reproducible from the companies index, never an input.
team-pages-distinct.csv
